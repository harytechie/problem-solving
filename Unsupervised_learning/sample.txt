'''
Unsupervised Learning

   1. What is Unsupervised Learning?

Unsupervised Learning is a type of Machine Learning where the model learns from data without predefined output labels.
Unsupervised Learning
|
├── 1. K-Means Clustering
|
└── 2. Hierarchical Clustering

K-Means Clustering:

->used to group similar data points into K groups (clusters).
->K-Means is an unsupervised learning algorithm used to divide data into K groups called clusters.
REAL TIME EXAMPLE:
House price grouping -> group based on(area,no of bedrooms,and price),
K means group such as budget houses,med priced ,luxury houses.
ADVANTAGES:
Simple and easy to understand,work fast,useful for many application(customer segmentation,image compression,doc grouping),
scalable(handle large amount of data).
Working of K-Means Clustering:
Example: Customer spending data:10, 12, 14, 50, 52, 55
Suppose K = 2.
If you want, I can also:

* Finish the step-by-step math for the working example where K = 2
* Explain the difference between K-Means and Hierarchical Clustering
* Provide a Python code snippet implementing this exact example

Let me know how you would like to proceed!

REAL TIME EXAMPLE:
House price grouping -> group based on(area,no of bedrooms,and price),
K means group such as budget houses,med priced ,luxury houses.
ADVANTAGES:
Simple and easy to understand,work fast,useful for many application(customer segmentation,image compression,doc grouping),
scalable(handle large amount of data).
Working of K-Means Clustering:
Example: Customer spending data:10, 12, 14, 50, 52, 55
Suppose K = 2.
Step 1: we decide how many groups we want
so, K=2 (we need 2 clusters)
Step 2: Select initial centroids
K-Means initially selects 2 points as centroids (cluster centers).
EXAMPLE:
centroid 1 = 10
centroid 2 = 50
Step 3: find distance between data point and centroid.
For 10:
Distance from C1 = |10 - 10| = 0
Distance from C2 = |10 - 50| = 40
-> 10 goes to Cluster 1
If you'd like, I can:

* Show you the math for the rest of the numbers (12, 14, 52, 55)
* Explain how to update the centroids for Step 4
* Write a quick Python script to run this calculation automatically

Let me know what you need next!

Again, K-Means checks:
Assign -> Calculate mean -> Move centroid -> Assign -> Calculate mean...
This continues until the centroids stop changing significantly.
CODE:
2
from sklearn.cluster import KMeans
## Data
X = [,
,
,
,
,
[55]
]
## Create K-Means model
model = KMeans(
n_clusters=2,
random_state=42,
n_init=2
)
## Train the model
model.fit(X)
## Cluster assigned to each value
print("Cluster labels:", model.labels_)
If you want, I can:

* Combine this new section with the previous study guide to create one complete file.
* Explain what the output labels (model.labels_) will look like when you run this code.
* Show you how to plot the results using matplotlib.

Let me know what you would like to do!

12

Types :
   1. Feature creation: creating new Feature from existing feature.
   price   quantity    total
   100      5           500
   200      3           600
   


'''